<!DOCTYPE html>
<html>
<head>
</head>
   <div class="container clearfix">
	 <header class="header clearfix">
     <nav>
      <ul class="top_menu clearfix">
       <li><%= link_to 'つぶやく', new_blog_path, class: 'btn btn-default' %></li>
      </ul>
     </nav>
    </header>

    <article class="content clearfix">
    <h1>つぶやいた一覧</h1>

    <p><%= notice %></p>

  <table>
  <tr>
    <th>つぶやき内容</th>
    <th colspan="3"><center>操作</center></th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.content %></td>
    <td><%= link_to '確認する', blog_path(blog.id) %></td>
    <td><%= link_to "編集する", edit_blog_path(blog.id) %></td>
    <td><%= link_to '削除する', blog_path(blog.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %></td>
  </tr>
<% end %>
</table>

<br>
<%= link_to 'つぶやき画面にもどる', new_blog_path, class: 'btn btn-default' %>
<br>
<%= link_to 'DIVE INTO CODEのページへ', 'https://diveintocode.jp/', class: 'btn btn-default' %>
</article>

<footer class="footer">
        <p>footer</p>
</footer>
</div>